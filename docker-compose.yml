version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: stalci_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: stalci_db
    ports:
      - 5435:5432
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
