# Phase 2: Backend API Plan

## Goal
Build the CMS, CRM, and Finance modules in NestJS.

## Tasks
1. [ ] Generate `cms` module (for Pages, Services, Blogs)
2. [ ] Generate `crm` module (for Inquiries, Feedback)
3. [ ] Generate `finance` module (for Invoices)
4. [ ] Setup Prisma ORM and SQLite/PostgreSQL configuration
5. [ ] Define entities/schemas for all modules
6. [ ] Implement basic CRUD API endpoints
