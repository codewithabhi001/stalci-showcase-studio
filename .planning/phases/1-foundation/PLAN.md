# Phase 1: Foundation Plan

## Goal
Initialize the new microservices architecture and fix the frontend bugs.

## Tasks
1. [x] Move `stalci-mark.png` to `public/` directory
2. [x] Update `Hero.tsx` to fix the logo import and fix responsive height constraints
3. [x] Initialize NestJS Backend in `/backend`
4. [x] Initialize Next.js Admin Panel in `/admin-panel`
